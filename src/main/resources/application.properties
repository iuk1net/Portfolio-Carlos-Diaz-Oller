# Server configuration
server.port=8080


# Database configuration
# Variables de entorno: DB_HOST, DB_PORT, DB_NAME, DB_USERNAME, DB_PASSWORD
# Valores por defecto solo para desarrollo local
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver


spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect


# Thymeleaf configuration
spring.thymeleaf.cache=false

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
app.upload.dir=uploads/cvs
app.upload.images.dir=uploads/images

# Logging configuration
logging.level.org.springframework=INFO
logging.level.com.yourpackage=INFO
logging.level.org.springframework.security=INFO
# logging.level.org.hibernate=DEBUG


spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=carlosiuka88@gmail.com
spring.mail.password=yguc ccvn dsja dclu

# Propiedades SMTP
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000


verificacion.email.expiracion-horas=24
verificacion.email.url-base=http://localhost:8089

# ========================================
# LINKEDIN CONFIGURATION (v3.0.0 - MVP)
# ========================================

linkedin.enabled=true
linkedin.test-mode=false


linkedin.publication-type=personal

# Organization ID (solo se usa si publication-type=organization)
# URL: https://www.linkedin.com/company/111341630/
linkedin.organization-id=111341630

# API Configuration
linkedin.api-base-url=https://api.linkedin.com/v2


linkedin.access-token=${LINKEDIN_ACCESS_TOKEN:}


linkedin.client.id=78agf1749a6gry
linkedin.client.secret=${LINKEDIN_CLIENT_SECRET:}


